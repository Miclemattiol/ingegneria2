export type credentials = {
    email: string,
    password: string
}

export type employee = {
    employeeId: String,
    email: String,
    password: String,
    name: String,
    surname: String,
    level: Number,
}

export type user = {
    userID: number,
    email: string,
    name: string,
    surname: string
}